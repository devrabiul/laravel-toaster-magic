<?php

/**
 * Marker written next to the published assets so the package can tell whether
 * the copy in public/ matches the installed release. Kept in sync with the
 * git tag; see AssetsServiceProvider::resolveAssetIdentity().
 */
return [
    'version' => 'v2.5',
];
